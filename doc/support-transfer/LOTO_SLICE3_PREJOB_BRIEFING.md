---
record_type: slice_prejob_briefing
slice: 3
target: CC_Loto
version: 1
recorded_at_utc: 2026-07-19T22:06:40Z
authorized_by: M3_APPROVAL.md items 5-8 and closed Slice 2 board-freshness boundary
implementer: codex
reviewer: claude-code
target_parent: 4ce96acb3a47d6239dd85abbedaa6d5bd5b7a38a
status: pre-write-review-required
---

# CC_Loto Slice 3 pre-job briefing v1 — handoff core

This packet opens independent pre-write review only. It authorizes no CC_Loto write, commit, push,
dependency change, product change, or M4 transition. Codex is implementer; Claude Code is the
independent reviewer.

## Exact content scope — seven creates plus one generated modification

Content commit A would copy the byte authority in [`rendered/loto-slice3/`](rendered/loto-slice3/)
exactly:

1. create `support/schemas/handoff.schema.json`
2. create `support/templates/handoff-record.template.md`
3. create `support/templates/handoff-pointer.template.md`
4. create `support/templates/continuity-checklist.template.md`
5. create `support/handoffs/README.md`
6. create `tools/support/make_handoff.py`
7. create `HANDOFF.md`
8. modify generated `coordination/BOARD.md`

The board modification is mandatory, not scope creep: creating `HANDOFF.md` changes the pointer
state, so the installed target validator rejects the old Slice 2 board as stale. The candidate board
is generated by the already published `tools/support/agent_coord.py` at the reviewed timestamp and
truthfully reports the bootstrap pointer.

No immutable handoff record, target-local message, claim, or archive record is created at install.
No agent-owned guidance, support index, test, workflow, dependency file, product source, data/model
output, index configuration, tag, or release is in Slice 3.

Exact review inputs:

- [`LOTO_SLICE3_RENDER_EVIDENCE.md`](LOTO_SLICE3_RENDER_EVIDENCE.md)
- [`LOTO_SLICE3_DRY_RUN_EVIDENCE.md`](LOTO_SLICE3_DRY_RUN_EVIDENCE.md)
- [`rendered/render_loto_slice3.py`](rendered/render_loto_slice3.py)
- [`rendered/loto-slice3/`](rendered/loto-slice3/)

## Verified preflight state

- CC_Loto local HEAD and `origin/main` equal closed Slice 2 tip
  `4ce96acb3a47d6239dd85abbedaa6d5bd5b7a38a`; divergence `0 0`; porcelain empty.
- Seven create paths are absent; the sole collision is `coordination/BOARD.md`.
- Fixed-timestamp rerender reproduced all eight hashes with zero differences.
- Disposable overlay validation returned zero errors and warnings.
- First publication replaced the bootstrap pointer and appended the canonical event; validation
  then failed on stale board and returned to zero only after installed-tool regeneration.
- The target-adapted publisher suite passed 33/33 outside the sandbox; the sandbox-failed attempt is
  disclosed separately.
- Native required short layers passed 70/70 outside the sandbox.

## Preflight immediately before any content commit A

1. Reverify `HEAD == origin/main == 4ce96ac...`, divergence `0 0`, and empty porcelain.
2. Reverify the seven create paths are absent and the existing board equals the published Slice 2
   blob.
3. Require Claude's explicit acceptance of the complete packet and generated-board scope.
4. Require the Wiki packet commit containing this exact byte authority to be pushed and identified.
5. Rerun the fixed-timestamp renderer and confirm 8/8 reviewed hashes plus the publisher lifecycle
   and 33/33 adapted tests.

Any mismatch is a stop condition.

## Proposed two-commit evidence protocol after acceptance

1. **Content commit A:** copy the seven creates and replace only `coordination/BOARD.md`; stage
   exactly eight paths; compare every staged blob against the reviewed source; commit locally.
2. **Validation at clean A:** run `python tools/support/agent_coord.py .` with the accepted support
   environment and require exit `0`, 0 errors, 0 warnings, bootstrap pointer named in the board, and
   no runtime artifact. Run the three target-native short layers and require 70/70.
3. **Evidence commit B:** modify only `support/log.md` and `support/current-status.md` to record A's
   full SHA, exact commands, integer exits, native counts, support environment, bootstrap pointer,
   generated-board truth, and remaining gates.
4. **Validation at clean B:** repeat coordination and native checks; verify A-byte identity,
   `B^ == A`, eight-path A scope, and two-path B scope.
5. Keep A and B local until Claude independently reviews committed objects and explicitly
   authorizes their exact push.

Rollback, only on reviewer/owner direction, is a new revert of B followed by A. Reset, force push,
broad cleanup, or restoration of unrelated paths is prohibited.

## Stop conditions

Stop on preflight drift; any path outside A's seven creates plus generated board or B's two evidence
modifications; byte mismatch; stale board; non-zero coordination validation; publisher lifecycle or
33-test regression; native short-layer regression; sensitive/private/product data; unresolved
non-template placeholder; dangling/escaping link; runtime artifact; or reviewer finding. M4 remains
closed.
