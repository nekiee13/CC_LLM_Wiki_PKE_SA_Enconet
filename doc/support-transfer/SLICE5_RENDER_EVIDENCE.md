# Slice-5 rendered-tree evidence

The exact candidate for CC_FIN content commit A is `rendered/slice5/`: two modifications
based on published parent `9841751e13213e3e8766f41ec2b140dd8dd8fd74`:
`AGENTS.md` and `docs/governance-transition.md`.

## Candidate checks

- **passed, exit 0** — renderer produced exactly the two authorized paths.
- **passed** — parent `pyproject.toml` proves setuptools backend, project `cc-fin` 2.1.0,
  and package discovery under `src/`; stale empty-file/build guidance was removed.
- **passed** — support navigation names every accepted continuity surface, preserves
  Codex/Claude ownership, and does not fork product or packaging authority.
- **passed** — divergence recovery is evidence-first and preservation-aware, requires
  explicit owner approval for destructive discard, and contains no executable hard-reset
  example or implicit destructive authorization.
- **passed, exit 0** — installed target coordination CLI validated a disposable accepted
  Slice-1 + Slice-2 + Slice-3 + Slice-3c + Slice-5 overlay with 0 errors and 0 warnings;
  BOARD bytes were identical before and after.
- **passed, exit 0** — workspace guidance drift: 0 errors, 3 pairs, 39 anchor rules,
  8 documented differences.
- **passed** — two consecutive renders produced 2/2 identical SHA-256 values:
  `6680CA0FEE9E686D131FF9C92338A80E505D9776C4D9801CBF6FB17A014D890E` and
  `969EBB514B2F0DE03CFD75EB18D9593FB1A15282033C4FCB55EADF1674EC16AB`.
- **passed** — sensitive-pattern, placeholder, forbidden-workspace-reference, required-text,
  and exact-inventory assertions completed inside the renderer.
- **observed** — live target was clean and synchronized at the reviewed parent with
  divergence `0 0` during package preparation.

## BOARD and ownership proof

The two proposed files are not board inputs, and the validator did not write the board.
No generated target file is concealed in the slice. Codex authored only its assigned guidance
and neutral governance proposal; no Claude-owned target or record is in the rendered tree.

## Pending target checks

No CC_FIN file changed while producing this evidence. Claude Code's pre-write review,
immediate live preflight, staged-blob identity, content A, target/native A validation,
evidence B, target/native B validation, final commit identities, and pre-push review remain
pending.
