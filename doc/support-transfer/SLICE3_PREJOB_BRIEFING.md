---
record_type: support_transfer_prejob_briefing
slice: 3
version: 1
recorded_at_utc: 2026-07-18T13:15:45Z
authorized_by: M2_APPROVAL.md, M2_AMENDMENT_1.md, and M2_AMENDMENT_2.md
implementer: codex
reviewer: claude-code
target_parent: d442373995b7dd114aa4837821cec2c6120b3b74
status: pre-write-review-required
---

# Slice-3 pre-job briefing — CC_FIN handoff core

## Authority and roles

The owner confirmed Codex as implementer and Claude Code as independent reviewer.
M2 amendment 2 resolves the slice-2 boundary finding: Slice 3 is exactly **7 creates
plus 1 generated modification**. The only existing target file allowed to change in
content commit A is `coordination/BOARD.md`, regenerated by the installed CLI with the
reviewed timestamp `2026-07-18T13:15:45Z`. No CC_FIN write occurs before Claude accepts
this briefing and exact tree.

The clean pre-slice parent is published Slice 2
`d442373995b7dd114aa4837821cec2c6120b3b74`. Recovery is a reviewed revert of Slice 3's
named commits to that parent, never reset or broad cleanup.

## Exact content-A byte authority

The committed tree `rendered/slice3/` contains exactly these eight paths:

1. create `support/schemas/handoff.schema.json`
2. create `support/templates/handoff-record.template.md`
3. create `support/templates/handoff-pointer.template.md`
4. create `support/templates/continuity-checklist.template.md`
5. create `support/handoffs/README.md`
6. create `scripts/make_handoff.py`
7. create `HANDOFF.md`
8. modify `coordination/BOARD.md` (generated evidence authorized by amendment 2)

No record is created in `support/handoffs/` during installation. `HANDOFF.md` is an
honest bootstrap pointer state derived from the pointer contract: it says that no
immutable handoff is published, contains no dangling link or fabricated record ID,
and instructs the next session to inspect current status/coordination/Git state. The
first successful `scripts/make_handoff.py` publication atomically replaces it with the
normal record link. The generated board therefore reports
`Record: none published (bootstrap state)` until that first publication.

## Disclosed target adaptations

The publisher is the accepted staged `handoff_publisher.py` with only target integration
changes:

- installed filename/module documentation replaces staged/Wiki wording;
- `from _shared ...` becomes `from _support_shared ...`, using the exact helper already
  installed by Slice 2;
- the publication append uses CC_FIN's canonical pipe-delimited event shape:
  `handoff-published | <UTC> | <record-id> | <statement> | <source-agent>` instead of
  the prototype's whitespace-only line.

The schema is unchanged. The record and pointer templates retain only the exact T5
placeholder allowlist. The continuity checklist renders `PROJECT_NAME` as `CC_FIN`.

## Reproduction and disposable evidence

Run from the Wiki root with a writable temporary directory:

`$env:PYTHONDONTWRITEBYTECODE='1'; python doc/support-transfer/rendered/render_slice3.py --timestamp 2026-07-18T13:15:45Z`

Expected exit 0: target coordination validation 0 errors/0 warnings; exact inventory
7 creates + 1 BOARD modification; first-record publication replaces the bootstrap
pointer and appends the target event; validation then fails on stale BOARD and returns
to 0 only after regeneration. A second fixed-timestamp render must reproduce all 8
files byte-for-byte.

The accepted staged publisher suite is run against the final rendered module through a
disposable alias harness. Expected: 33 passed, exit 0. The harness and reports remain
outside the rendered tree and target.

## Preflight immediately before target write

1. Fetch/inspect live CC_FIN `origin/main`; require local HEAD, tracking branch, and
   remote main all equal `d442373995b7dd114aa4837821cec2c6120b3b74`, divergence
   `0 0`, and empty porcelain.
2. Require all seven create paths absent and `coordination/BOARD.md` present at the
   reviewed Slice-2 blob.
3. Re-run the fixed renderer; require exact eight-file inventory, byte reproduction,
   target validator 0/0, publisher lifecycle probe success, and 33/33 fault tests.
4. Stop on any upstream drift, target presence collision, source/byte drift, sensitive
   hit, unknown non-template placeholder, dangling/escaping link, or runtime artifact.

## Two-commit target protocol

1. **Content commit A**: copy the seven creates and replace only
   `coordination/BOARD.md` from `rendered/slice3/`; stage exactly the eight paths;
   compare every staged blob to the reviewed source; commit only on 8/8 identity.
2. **Target validation at clean A**: run exactly
   `$env:PYTHONDONTWRITEBYTECODE='1'; python scripts/agent_coord.py .`; require exit 0,
   `validate: 0 error(s), 0 warning(s)`, the bootstrap pointer named in BOARD, and no
   runtime artifact.
3. **Native validation at clean A**: run exactly
   `$env:PYTHONDONTWRITEBYTECODE='1'; python -m pytest -p no:cacheprovider --continue-on-collection-errors -q --tb=no --junitxml="$env:TEMP\fin_slice3_A.xml"`.
   Expected exit 1 only because the committed normative baseline remains 54 tuples:
   343 tests, 276 passed, 51 failed, 3 collection errors, 13 skipped. Require 54/54,
   0 new, 0 gone, 0 mutated.
4. **Evidence commit B**: append `support-committed-local` and `support-validated`
   events for `SLICE-3` to `support/log.md` and refresh only
   `support/current-status.md`. Record A's full SHA and committer UTC, both literal
   validation commands and integer exit codes, target 0/0, and native tuple evidence.
5. At clean B, rerun the same target command and native command using
   `fin_slice3_B.xml`; require the same target verdict, the same 54 tuples, and an
   A-to-B identical tuple set.
6. Confirm A is exactly the eight reviewed blobs, `B^ == A`, and `A..B` changes exactly
   `support/current-status.md` and `support/log.md`. Submit A/B and all evidence to
   Claude; no CC_FIN push occurs before independent acceptance.

Any scope, byte, validation, baseline, pointer, board, event-format, or reviewer defect
is a stop. Report the literal failed command and integer exit code; do not relabel an
expected native failure as a pass.
